# ספר פרויקט גמר
## Wedding Invite Manager — מנהל הזמנות חתונה
### פרויקט תכנה ו-DevOps עם בינה מלאכותית

---

| | |
|---|---|
| **שם הסטודנט** | _____________ |
| **ת.ז.** | _____________ |
| **מסלול** | הנדסאי תוכנה |
| **מגמה** | DevOps & Software Engineering |
| **מכללה** | _____________ |
| **שנת הגשה** | 2026 |

---

## תוכן עניינים

1. [רקע — DevOps ו-CI/CD](#1-רקע--devops-וcicd)
2. [מטרת הפרויקט](#2-מטרת-הפרויקט)
3. [סקירת מצב קיים בשוק](#3-סקירת-מצב-קיים-בשוק)
4. [תיאור האפליקציה](#4-תיאור-האפליקציה)
5. [ארכיטקטורת המערכת](#5-ארכיטקטורת-המערכת)
6. [טכנולוגיות בשימוש](#6-טכנולוגיות-בשימוש)
7. [מבנה קבצי הפרויקט](#7-מבנה-קבצי-הפרויקט)
8. [בסיס הנתונים](#8-בסיס-הנתונים)
9. [הממשק הגרפי — REST API](#9-הממשק-הגרפי--rest-api)
10. [שילוב בינה מלאכותית](#10-שילוב-בינה-מלאכותית)
11. [שליחת WhatsApp — Green API](#11-שליחת-whatsapp--green-api)
12. [אריזה בדוקר](#12-אריזה-בדוקר)
13. [Nginx — שרת הפרוקסי](#13-nginx--שרת-הפרוקסי)
14. [ניטור — Prometheus ו-Grafana](#14-ניטור--prometheus-וgrafana)
15. [אחסון פריטים רגישים — Secrets](#15-אחסון-פריטים-רגישים--secrets)
16. [בדיקות — pytest](#16-בדיקות--pytest)
17. [תהליך ה-CI/CD — GitHub Actions](#17-תהליך-הcicd--github-actions)
18. [פריסה בענן — AWS EC2 עם Terraform](#18-פריסה-בענן--aws-ec2-עם-terraform)
19. [בעיות שהתגלו ופתרונות](#19-בעיות-שהתגלו-ופתרונות)
20. [סיכום](#20-סיכום)

---

## 1. רקע — DevOps ו-CI/CD

### מהו DevOps?

DevOps הוא גישת פיתוח, תפעול וניהול תוכנה המשלבת בין צוותי הפיתוח (Development) לצוותי התפעול (Operations). הגישה דוגלת בשלושה עקרונות מרכזיים:

- **אוטומציה** — כל שלב שניתן לאוטמט, יש לאוטמט: בנייה, בדיקה, פריסה
- **שחרור מהיר של גרסאות** — delivery מתמיד ותכוף לסביבת הייצור
- **שיתוף פעולה** — פיתוח ותפעול עובדים יחד תוך נראות מלאה

### מהו CI/CD?

**CI — Continuous Integration (אינטגרציה מתמדת):**
בכל פעם שמפתח דוחף קוד לריפוסיטורי, מופעל אוטומטית תהליך שכולל:
1. הורדת קבצי הקוד החדש
2. התקנת ספריות (dependencies)
3. הרצת חבילת הבדיקות
4. דיווח — הצלחה או כישלון

**CD — Continuous Deployment (פריסה מתמדת):**
אם כל הבדיקות עברו, התהליך ממשיך אוטומטית:
1. בניית Docker Image מהקוד
2. דחיפת ה-Image ל-Registry פרטי
3. פריסה אוטומטית לשרת הייצור בענן

```
מפתח כותב קוד
      ↓
  git push → GitHub
      ↓
GitHub Actions מופעל
      ↓
  ┌───────────┐   נכשל   ┌──────────────┐
  │   pytest  │ ────────▶│ email לצוות  │
  └───────────┘          └──────────────┘
      │ עבר
      ↓
  Docker build + push
      ↓
  SSH → EC2
      ↓
  docker-compose up -d
      ↓
  האפליקציה החדשה פועלת
```

### למה DevOps חשוב?

בעולם המסורתי, צוות הפיתוח היה מוסר חבילה לצוות התפעול פעם בחודשים. הבעיות היו:
- קשה לאתר באגים בגלל כמות השינויים הגדולה
- פריסה ידנית — מועדת לטעויות אנוש
- זמן ממושך בין כתיבת קוד לפריסה

גישת DevOps פותרת זאת: כל שינוי קוד — אפילו שורה אחת — עובר דרך הפייפליין ונבדק ונפרס אוטומטית.

---

## 2. מטרת הפרויקט

### אפליקציית Wedding Invite Manager

הפרויקט מדגים תהליך DevOps מלא על אפליקציה אמיתית ושימושית: **מנהל הזמנות חתונה**.

**הרעיון:** בחתונה ישראלית, יש עשרות עד מאות אורחים שצריך להזמין. שליחת הזמנה אישית לכל אחד בנפרד לוקחת שעות. האפליקציה מאפשרת:

1. **ניהול רשימת אורחים** — הוספה, עריכה, מחיקה, קיבוץ לפי קשרים (משפחה, חברים, עבודה)
2. **יצירת הזמנה מותאמת אישית** — בינה מלאכותית (Google Gemini) כותבת הודעה שונה לכל אורח בהתאם לקשר שלו לזוג
3. **שליחת הזמנה ב-WhatsApp** — דרך Green API, לכולם בבת אחת
4. **שליחת תזכורת** — מספר ימים לפני האירוע, באותו מנגנון
5. **מעקב** — האפליקציה שומרת מי קיבל הזמנה, מי קיבל תזכורת, ומציגה סטטוס עדכני

**מטרת DevOps:**
- להדגים פייפליין CI/CD מלא מ-code ל-production
- לנהל סביבות שונות (local / EC2)
- לאמן אוטומציה, מוניטורינג ופריסה בענן

---

## 3. סקירת מצב קיים בשוק

כיום, בתחום שליחת הזמנות חתונה, הפתרונות הנפוצים הם:
- **הדפסה וחלוקה פיזית** — יקר, לא מדיד
- **שליחה ידנית ב-WhatsApp** — שוגה, לוקחת שעות
- **שירותי SMS** — יקרים לכמויות גדולות, אין עיצוב

**הפרויקט שלנו מחדש:**
- שליחה אוטומטית לכל הרשימה בלחיצת כפתור
- הודעה אישית לכל אורח — לא הודעה גנרית
- מעקב מי קיבל ומי לא
- מחיר נמוך (Green API — תשלום לפי הודעה)

---

## 4. תיאור האפליקציה

### ממשק המשתמש

האפליקציה היא Web Application המורכבת מ-5 מסכים:

| מסך | תיאור |
|-----|-------|
| Dashboard | סקירה כללית — מספר אורחים, כמה הוזמנו, כמה עוד לא |
| ניהול אורחים | רשימת כל האורחים עם אפשרות עריכה, מחיקה וסינון לפי קבוצה |
| הוספת אורח | טופס הוספה / עריכה של אורח |
| הגדרות אירוע | שמות הזוג, תאריך, מקום, ועלאת תמונת הזמנה |
| שליחת הזמנות | בחירת אורחים ושליחת הזמנה / תזכורת |

### זרימת נתונים

```
משתמש (Browser)
      │
      ▼
Nginx (Reverse Proxy)  ← קבצים סטטיים ישר מ-Nginx
      │
      ▼
Flask Application (Gunicorn, 4 workers)
      │
      ├──▶ MySQL Database  ← אורחים, אירועים, לוגי הודעות
      │
      ├──▶ Google Gemini   ← יצירת טקסט ההזמנה
      │
      └──▶ Green API       ← שליחת WhatsApp
```

---

## 5. ארכיטקטורת המערכת

### שכבות (Layers)

**שכבה 1 — Frontend/Proxy: Nginx**
- מאזין על פורט 80 (EC2) / 8080 (local)
- מגיש קבצים סטטיים (CSS, images) ישירות — מפחית עומס מ-Flask
- פועל כ-Reverse Proxy: מעביר בקשות לשרת Flask הפנימי
- מהווה שכבת הגנה — Flask לא חשוף ישירות לאינטרנט

**שכבה 2 — Backend: Flask + Gunicorn**
- Flask: Web Framework — מגדיר routes (URLs) וטיפול בבקשות
- Gunicorn: WSGI Server — מריץ Flask עם 4 workers מקביליים
- SQLAlchemy: ORM — מתרגם Python objects ל-SQL queries
- Prometheus Exporter: חושף metrics של ביצועים ב-`/metrics`

**שכבה 3 — Database: MySQL 8**
- שלוש טבלאות: guests, events, message_logs
- רץ בקונטיינר נפרד עם volume קבוע (data לא אובד בהפעלה מחדש)
- health-check מוגדר — Flask לא מתחיל לפני שDB מוכן

**שכבה 4 — Monitoring: Prometheus + Grafana**
- Prometheus: אוסף metrics מה-Flask כל 15 שניות
- Grafana: לוח בקרה ויזואלי עם גרפים בזמן אמת

### Docker Compose — תרשים קונטיינרים

```
┌─────────────────── Docker Network: app-net ──────────────────┐
│                                                               │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐               │
│  │  nginx   │───▶│   app    │───▶│  mysql   │               │
│  │  :8080   │    │  :5000   │    │  :3306   │               │
│  └──────────┘    └──────────┘    └──────────┘               │
│                       │                                       │
│              ┌────────┴────────┐                             │
│              ▼                 ▼                             │
│       ┌──────────┐    ┌──────────────┐                      │
│       │prometheus│    │   grafana    │                       │
│       │  :9090   │    │    :3000     │                       │
│       └──────────┘    └──────────────┘                      │
└───────────────────────────────────────────────────────────────┘
         │                │
         ▼                ▼
   (exposed to host)  (exposed to host)
```

---

## 6. טכנולוגיות בשימוש

### Flask
Web Framework בפייתון. מאפשר הגדרת routes (URLs) בפשטות, ניהול בקשות HTTP ותגובות JSON. בפרויקט, Flask משמש לבניית REST API ולהגשת דפי HTML (Jinja2 templates).

### Docker
כלי לאריזה (containerization). כל שירות (Flask, MySQL, Nginx, Prometheus, Grafana) רץ בקונטיינר מבודד עם כל הספריות שלו. כך:
- "works on my machine" — נפתר. אותו image רץ זהה בכל מקום
- ניתן להעלות ולהוריד שירותים בנפרד
- הגדרה מרכזית ב-`docker-compose.yml`

### Nginx
שרת פרוקסי (Reverse Proxy). מקבל בקשות מהדפדפן ומעביר אותן ל-Flask. יתרונות:
- Flask לא חשוף ישירות לאינטרנט
- Nginx מגיש קבצים סטטיים מהיר מאוד (ללא עבור דרך Flask)
- Docker resolver מוגדר (`127.0.0.11`) לפתרון שמות קונטיינרים

### MySQL 8
בסיס נתונים רלציוני. שלוש טבלאות בפרויקט: אורחים, אירוע, לוגי הודעות. SQLAlchemy מנהל את הקשר בין Flask ל-MySQL.

### Google Gemini AI
מודל בינה מלאכותית מבית Google. בפרויקט, הוא כותב הודעות WhatsApp בעברית — מותאמות אישית לכל אורח לפי שם, קשר לזוג (משפחה/חברים/עבודה) ופרטי האירוע. פועל גם ללא API key (מצב fallback עם תבנית קבועה).

### Green API
שירות המאפשר שליחת הודעות WhatsApp דרך API. בישראל, Green API הוא הפתרון הנפוץ והנגיש ביותר. פורמט המספר: `972521234567@c.us`. ללא API key — האפליקציה פועלת במצב mock (מדפיסה ללוג).

### Prometheus + Grafana
**Prometheus:** כלי ניטור שאוסף מדדים (metrics) מהאפליקציה דרך endpoint `/metrics` כל 15 שניות — כמות בקשות, זמני תגובה, שגיאות.
**Grafana:** לוח בקרה ויזואלי המציג גרפים בזמן אמת על בסיס הנתונים מ-Prometheus.

### GitHub Actions
מערכת CI/CD המובנית ב-GitHub. pipeline מוגדר בקובץ YAML (`.github/workflows/ci.yml`). מופעל אוטומטית בכל `git push` ל-main.

### Terraform
כלי לניהול תשתיות כקוד (Infrastructure as Code). מגדיר את כל משאבי ה-AWS (EC2, Security Group) בקבצי HCL. פקודה אחת — `terraform apply` — מקים את כל הסביבה בענן.

### pytest
Framework לבדיקות בפייתון. הבדיקות בפרויקט רצות על SQLite in-memory (ללא MySQL) ובודקות הכנסה/הוצאה דרך שרת Flask.

---

## 7. מבנה קבצי הפרויקט

```
wedding-invite-manager/
│
├── app/                          # הקוד הראשי של האפליקציה
│   ├── __init__.py               # Flask factory function — יוצר את האפליקציה
│   ├── config.py                 # הגדרות מסביבת הריצה (.env)
│   ├── models/
│   │   ├── guest.py              # מודל Guest (SQLAlchemy)
│   │   ├── event.py              # מודל Event
│   │   └── message_log.py        # מודל MessageLog
│   ├── controllers/
│   │   ├── guest_controller.py   # לוגיקה: CRUD אורחים, שליחת הזמנות
│   │   └── event_controller.py   # לוגיקה: ניהול אירוע
│   ├── routes/
│   │   ├── api_routes.py         # REST API endpoints
│   │   └── ui_routes.py          # HTML page routes
│   ├── services/
│   │   ├── ai_service.py         # Google Gemini — יצירת הודעות
│   │   └── whatsapp_service.py   # Green API — שליחת WhatsApp
│   ├── templates/                # HTML (Jinja2)
│   │   ├── base.html
│   │   ├── dashboard.html
│   │   ├── guests.html
│   │   ├── guest_form.html
│   │   ├── event_settings.html
│   │   └── send_invitations.html
│   └── static/css/style.css
│
├── tests/
│   ├── conftest.py               # pytest fixtures (SQLite in-memory)
│   └── test_api.py               # בדיקות CRUD + health
│
├── nginx/
│   └── nginx.conf                # Reverse proxy + static files
│
├── monitoring/
│   └── prometheus.yml            # Scrape config
│
├── terraform/
│   ├── main.tf                   # EC2 + Security Group
│   ├── variables.tf              # משתני קלט
│   └── outputs.tf                # פלטים (IP, URLs)
│
├── .github/workflows/
│   └── ci.yml                    # GitHub Actions pipeline
│
├── Dockerfile                    # בניית Flask image
├── docker-compose.yml            # הרכבת כל השירותים
├── requirements.txt              # תלויות פייתון
├── run.py                        # נקודת כניסה (Gunicorn)
├── start.ps1                     # סקריפט הפעלה Windows
├── start.sh                      # סקריפט הפעלה Linux/Mac
└── Makefile                      # פקודות נוחות (make run/test/clean)
```

---

## 8. בסיס הנתונים

### טבלת guests
| עמודה | סוג | תיאור |
|-------|-----|-------|
| id | INTEGER PK | מזהה ייחודי |
| name | VARCHAR(100) | שם האורח |
| phone | VARCHAR(20) UNIQUE | מספר טלפון |
| group_name | VARCHAR(50) | קבוצה: family / friends / work |
| invitation_sent | BOOLEAN | האם הוזמן |
| invitation_sent_at | DATETIME | מתי |
| reminder_sent | BOOLEAN | האם קיבל תזכורת |
| reminder_sent_at | DATETIME | מתי |
| notes | TEXT | הערות |
| created_at | DATETIME | תאריך יצירה |

### טבלת events
| עמודה | סוג | תיאור |
|-------|-----|-------|
| id | INTEGER PK | מזהה |
| bride_name | VARCHAR(100) | שם הכלה |
| groom_name | VARCHAR(100) | שם החתן |
| wedding_date | DATETIME | תאריך ושעת החתונה |
| venue | VARCHAR(200) | שם האולם |
| venue_address | TEXT | כתובת |
| invitation_image_path | VARCHAR(300) | נתיב לתמונת הזמנה |
| custom_message | TEXT | הודעה מותאמת אישית |
| reminder_days_before | INTEGER | כמה ימים לפני לשלוח תזכורת |

### טבלת message_logs
| עמודה | סוג | תיאור |
|-------|-----|-------|
| id | INTEGER PK | מזהה |
| guest_id | INTEGER FK | קשר לאורח |
| message_type | VARCHAR(20) | invitation / reminder |
| status | VARCHAR(20) | sent / failed |
| message_body | TEXT | תוכן ההודעה / שגיאה |
| sent_at | DATETIME | מתי נשלח |

---

## 9. הממשק הגרפי — REST API

### Endpoints של ה-API

| Method | URL | תיאור |
|--------|-----|-------|
| GET | `/api/guests` | כל האורחים (תמיכה ב-?group= ?status= ?sort=) |
| GET | `/api/guests/<id>` | אורח ספציפי |
| POST | `/api/guests` | יצירת אורח חדש |
| PUT | `/api/guests/<id>` | עדכון אורח |
| DELETE | `/api/guests/<id>` | מחיקת אורח |
| GET | `/api/event` | פרטי האירוע |
| PUT | `/api/event` | עדכון פרטי האירוע |
| POST | `/api/send/invitations` | שליחת הזמנות |
| POST | `/api/send/reminders` | שליחת תזכורות |
| GET | `/api/health` | בדיקת תקינות |
| GET | `/metrics` | Prometheus metrics |

---

## 10. שילוב בינה מלאכותית

### Google Gemini AI — יצירת הודעות

שירות `ai_service.py` אחראי על יצירת ההודעות. הוא משתמש ב-`gemini-2.0-flash` — מודל מהיר וחסכוני.

**לוגיקת ה-prompt:**
```python
tone = {
    "family": "חם ואישי מאוד, כמו לבן משפחה קרוב",
    "friends": "קליל ושמח, עם הומור קל",
    "work":    "מכובד אך ידידותי",
}.get(group, "ידידותי")

prompt = (
    f"כתוב הזמנה לחתונה עבור {guest_name}.\n"
    f"החתן: {groom}, הכלה: {bride}\n"
    f"תאריך: {date}, מקום: {venue}\n"
    f"סגנון: {tone}. עד 5 שורות, עם אימוג'ים."
)
```

**Fallback ללא API Key:**
```
שלום [שם] 💍
אנחנו שמחים להזמינך לחגוג איתנו את חתונתנו!
👰 [כלה] & 🤵 [חתן]
📅 [תאריך] | 📍 [מקום]
מחכים לראותך! ❤️
```

---

## 11. שליחת WhatsApp — Green API

### whatsapp_service.py

Green API מספק REST API לשליחת הודעות WhatsApp. הפרויקט תומך בשני סוגי שליחה:

**שליחת טקסט:**
```
POST https://api.green-api.com/waInstance{INSTANCE_ID}/sendMessage/{TOKEN}
Body: { "chatId": "972521234567@c.us", "message": "..." }
```

**שליחת תמונה עם כיתוב:**
```
POST https://api.green-api.com/waInstance{INSTANCE_ID}/sendFileByUrl/{TOKEN}
Body: { "chatId": "...", "urlFile": "...", "caption": "..." }
```

**המרת פורמט טלפון:**
```python
def _format_phone(phone):
    # +972521234567 → 972521234567@c.us
    # 0521234567    → 972521234567@c.us
```

**Mock Mode:** אם אין API key, כל שליחה מדפיסה ללוג ומחזירה `{"status": "sent", "mock": True}`.

---

## 12. אריזה בדוקר

### Dockerfile

```dockerfile
FROM python:3.11-slim       # Image בסיס קטן וקליל

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt  # שכבה נפרדת = cache

COPY . .
RUN mkdir -p app/static/uploads

EXPOSE 5000

CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "4", "--timeout", "120", "run:app"]
```

**עקרונות שהופעלו:**
- `python:3.11-slim` — image קטן, זמן build מהיר
- COPY requirements לפני הקוד — Docker cache לא מתבטל בכל שינוי קוד
- Gunicorn עם 4 workers — תמיכה בבקשות מקביליות

### docker-compose.yml

מגדיר 5 שירותים:

| שירות | Image | פורט |
|-------|-------|------|
| app | Dockerfile (local build) | 5000 (פנימי) |
| mysql | mysql:8.0 | 3306 (פנימי) |
| nginx | nginx:1.25-alpine | 8080→80 |
| prometheus | prom/prometheus:v2.52.0 | 9090 |
| grafana | grafana/grafana:10.4.3 | 3000 |

**Health Check ל-MySQL:**
```yaml
healthcheck:
  test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  interval: 10s
  retries: 10
```
Flask ממתין (depends_on: condition: service_healthy) עד שה-DB מוכן לגמרי.

---

## 13. Nginx — שרת הפרוקסי

### nginx.conf

```nginx
resolver 127.0.0.11 valid=30s;  # Docker internal DNS

server {
    listen 80;

    # קבצים סטטיים — Nginx מגיש ישירות (מהיר)
    location /static/uploads/ {
        alias /app/static/uploads/;
        expires 7d;
    }

    # שאר הבקשות → Flask
    location / {
        set $upstream http://app:5000;  # DNS resolve בזמן ריצה
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
    }
}
```

**מדוע `resolver 127.0.0.11`?**
בלי resolver, Nginx מנסה לפתור את שם הקונטיינר `app` בהפעלה — ואם Flask עוד לא עלה, Nginx קורס. עם Docker DNS resolver ומשתנה `$upstream`, הפתרון מתבצע בכל בקשה בנפרד.

---

## 14. ניטור — Prometheus ו-Grafana

### Prometheus

Prometheus אוסף metrics מהאפליקציה דרך endpoint `/metrics` שנוצר אוטומטית על ידי `prometheus-flask-exporter`.

**prometheus.yml:**
```yaml
scrape_configs:
  - job_name: "flask-app"
    static_configs:
      - targets: ["app:5000"]
    metrics_path: "/metrics"
    scrape_interval: 15s
```

**מדדים שנאספים:**
- `flask_http_request_duration_seconds` — זמן תגובה לכל endpoint
- `flask_http_request_total` — מספר בקשות לפי endpoint וסטטוס
- `flask_exporter_info` — מידע על הגרסה

### Grafana

- ניגש ל-Prometheus כ-data source
- מציג גרפים: בקשות בשנייה, זמן תגובה ממוצע, שגיאות 4xx/5xx
- גישה: `http://localhost:3000` (admin / admin123)

---

## 15. אחסון פריטים רגישים — Secrets

### GitHub Secrets

בתהליך CI/CD, המידע הרגיש מאוחסן ב-Settings → Secrets ב-GitHub:

| Secret | שימוש |
|--------|-------|
| `DOCKER_USERNAME` | התחברות ל-Docker Hub |
| `DOCKER_PASSWORD` | התחברות ל-Docker Hub |
| `SERVER_HOST` | כתובת IP של שרת Hetzner |
| `SERVER_SSH_KEY` | מפתח SSH פרטי לחיבור ל-Hetzner |
| `MAIL_USERNAME` | Gmail לשליחת התראות כישלון |
| `MAIL_PASSWORD` | App Password ל-Gmail |
| `MAIL_TO` | כתובת יעד לקבלת ההתראות |

### .env מקומי

בסביבה המקומית, קובץ `.env` (שאינו נכנס ל-Git בגלל `.gitignore`) מכיל:
```env
SECRET_KEY=...
GREEN_API_INSTANCE_ID=...
GREEN_API_TOKEN=...
GOOGLE_AI_API_KEY=...
```

### עקרון הסוד המינימלי

- קוד הפרויקט לא מכיל אף API key
- ב-Docker Compose, המשתנים מגיעים מ-`.env`
- בפייפליין, המשתנים מגיעים מ-GitHub Secrets
- Terraform משתמש ב-`sensitive = true` למשתנים הרגישים

---

## 16. בדיקות — pytest

### אסטרטגיית הבדיקה

הבדיקות בפרויקט עוברות דרך שרת Flask אמיתי (test client), ומשתמשות ב-**SQLite in-memory** במקום MySQL. כך:
- אין תלות ב-MySQL בסביבת CI
- כל בדיקה מתחילה עם DB נקי (autouse fixture)
- הבדיקות מהירות ומבודדות

### conftest.py

```python
TEST_CONFIG = {
    "TESTING": True,
    "SQLALCHEMY_DATABASE_URI": "sqlite:///:memory:",
    "SECRET_KEY": "test-secret",
    "GREEN_API_INSTANCE_ID": "",  # mock mode
    "GREEN_API_TOKEN": "",
    "GOOGLE_AI_API_KEY": "",      # fallback template
}
```

### test_api.py — רשימת בדיקות

| בדיקה | מה בודק |
|-------|---------|
| `test_create_guest` | יצירת אורח ← 201, שדות נכונים |
| `test_insert_and_retrieve_guest_from_db` | **הכנסה/הוצאה מה-DB דרך השרת** |
| `test_get_guest_by_id` | GET ספציפי לפי ID |
| `test_update_guest` | PUT — עדכון שם |
| `test_delete_guest` | DELETE — אורח נמחק |
| `test_filter_guests_by_group` | סינון לפי קבוצה |
| `test_create_and_retrieve_event` | PUT + GET event |
| `test_health_endpoint` | `/api/health` ← 200, status ok |

**הרצת בדיקות:**
```bash
pytest tests/ -v
```

---

## 17. תהליך ה-CI/CD — GitHub Actions

### הפייפליין (ci.yml)

הפייפליין מורכב מ-3 שלבים שרצים בסדר:

```
┌─────────────────────────────────────────────────────┐
│  Trigger: git push לענף main                        │
└───────────────────────┬─────────────────────────────┘
                        │
              ┌─────────▼──────────┐
              │  Stage 1: Test     │
              │  • Python 3.11     │
              │  • pip install     │
              │  • pytest tests/   │
              └─────────┬──────────┘
                        │                    ┌──────────────────┐
                 fail ──┼───────────────────▶│  Send email      │
                        │ pass               │  (Gmail SMTP)    │
              ┌─────────▼──────────┐         └──────────────────┘
              │ Stage 2: Build     │
              │ • docker/login     │
              │ • docker build     │
              │ • push :latest     │
              │ • push :SHA        │
              └─────────┬──────────┘
                        │
              ┌─────────▼──────────┐
              │ Stage 3: Deploy    │
              │ • SSH → Hetzner    │
              │ • git pull         │
              │ • docker pull      │
              │ • docker-compose   │
              │   up -d            │
              └────────────────────┘
```

### פרטי כל שלב

**Stage 1 — Test:**
- מריץ על `ubuntu-latest` (GitHub-hosted runner)
- מתקין Python 3.11 ואת כל הספריות
- מריץ `pytest tests/ -v`
- אם נכשל: שולח email עם פרטי הכישלון, link ל-run, שם ה-commit ומי דחף

**Stage 2 — Build & Push:**
- רץ רק אם Stage 1 עבר (`needs: test`)
- רק ב-push ל-main (לא ב-pull request)
- בונה Docker image ודוחף ל-Docker Hub עם שני תגיות:
  - `:latest` — תמיד מעודכן
  - `:SHA` — fingerprint של הקומיט (לחזרה לאחור)

**Stage 3 — Deploy:**
- מתחבר לשרת Hetzner דרך SSH (username: root)
- מושך קוד עדכני מ-GitHub
- מושך image עדכני מ-Docker Hub
- `docker-compose up -d --no-deps --build app` — zero-downtime rolling update

---

## 18. פריסה בענן — Hetzner Cloud עם Terraform

### מדוע Hetzner?

Hetzner Cloud הוא ספק ענן אירופאי המציע שרתים במחיר נמוך משמעותית מ-AWS. שרת CX22 (2 vCPU, 4 GB RAM) עולה כ-4 אירו בחודש — מתאים מצוין לפרויקט לימודי עם ביצועים אמיתיים.

### Terraform — Infrastructure as Code

Terraform מאפשר לכתוב את כל התשתית כקוד (HCL). במקום להיכנס ל-Hetzner Console ולהגדיר ידנית, פקודה אחת מקימה הכל.

**main.tf — המשאבים שנוצרים:**

**1. hcloud_ssh_key — מפתח SSH:**
מעלה את המפתח הציבורי שלך ל-Hetzner ומשייך אותו לשרת.

**2. hcloud_firewall — חומת אש:**
```
Port 22   (SSH)     — גישת ניהול
Port 80   (HTTP)    — האפליקציה
Port 3000 (Grafana) — לוח הניטור
```

**3. hcloud_server — השרת:**
- סוג: `cx22` — 2 vCPU / 4 GB RAM
- Image: Ubuntu 22.04
- מיקום: `nbg1` (Nuremberg) / `hel1` (Helsinki)
- user_data script — רץ אוטומטית בהפעלה הראשונה:

```bash
#!/bin/bash
apt-get update -y
apt-get install -y docker.io docker-compose git
systemctl enable docker && systemctl start docker

# Clone הפרויקט
cd /root
git clone https://github.com/${github_repo} wedding-invite-manager
cd wedding-invite-manager

# יצירת .env — פורט 80 לשרת
cat > .env <<EOF
SECRET_KEY=...
GREEN_API_INSTANCE_ID=...
GREEN_API_TOKEN=...
GOOGLE_AI_API_KEY=...
NGINX_PORT=80
EOF

docker-compose up -d
```

**outputs.tf:**
```hcl
output "server_ip"   { value = hcloud_server.wedding_server.ipv4_address }
output "app_url"     { value = "http://${hcloud_server.wedding_server.ipv4_address}" }
output "grafana_url" { value = "http://${...}:3000" }
```

**הרצת Terraform:**
```bash
terraform init      # הורדת hcloud provider
terraform plan      # תצוגה מקדימה
terraform apply \
  -var="hcloud_token=TOKEN" \
  -var="ssh_public_key=$(cat ~/.ssh/id_rsa.pub)" \
  -var="github_repo=user/repo" \
  ...
terraform destroy   # הרס הכל
```

### פורט דינמי — NGINX_PORT

ב-docker-compose.yml, פורט Nginx מוגדר כ:
```yaml
ports:
  - "${NGINX_PORT:-8080}:80"
```
- **מקומי (dev):** ברירת מחדל 8080 → `http://localhost:8080`
- **Hetzner (prod):** `NGINX_PORT=80` ב-.env → `http://SERVER_IP`

---

## 19. בעיות שהתגלו ופתרונות

### 1. Nginx לא מצליח לפתור שם קונטיינר בהפעלה

**הבעיה:** Nginx ניסה לפתור את שם `app` בזמן ה-startup — לפני שקונטיינר Flask עלה. Nginx קרס.

**הפתרון:** הוספת Docker internal DNS resolver והשמת ה-upstream במשתנה:
```nginx
resolver 127.0.0.11 valid=30s;
set $upstream http://app:5000;
proxy_pass $upstream;
```
כך הפתרון מתבצע לכל בקשה בנפרד, לא בהפעלה.

### 2. Terraform הכיל משתני Twilio אחרי מעבר ל-Green API

**הבעיה:** קבצי Terraform (`variables.tf`, `main.tf`) עדיין הגדירו `twilio_sid`, `twilio_token`, `twilio_from`, בזמן שהקוד עבר לשימוש ב-Green API.

**הפתרון:** הוחלפו כל משתני Twilio ב-`green_api_instance_id` ו-`green_api_token` בשני הקבצים.

### 3. conftest.py הכיל TWILIO_ACCOUNT_SID

**הבעיה:** `TEST_CONFIG` בבדיקות הגדיר משתנים שהאפליקציה כבר לא קוראת (`TWILIO_ACCOUNT_SID`, `OPENAI_API_KEY`).

**הפתרון:** עדכון ל-`GREEN_API_INSTANCE_ID`, `GREEN_API_TOKEN`, `GOOGLE_AI_API_KEY`.

### 4. README ציין פורט 80 בזמן שdocker-compose פתח 8080

**הבעיה:** README הפנה ל-`http://localhost` (פורט 80), בזמן שב-docker-compose מוגדר `"8080:80"`.

**הפתרון:** עדכון README ל-`http://localhost:8080` לסביבה מקומית, עם הבהרה שב-EC2 Nginx רץ על 80 ישירות.

### 5. Flask מתחיל לפני MySQL מוכן

**הבעיה:** Docker Compose מעלה שירותים במקביל; Flask ניסה להתחבר ל-MySQL לפני שה-DB סיים initialization.

**הפתרון:** הוספת `healthcheck` ל-MySQL ו-`depends_on: condition: service_healthy` ל-Flask:
```yaml
depends_on:
  mysql:
    condition: service_healthy
```

---

## 20. סיכום

### מה הושג

הפרויקט הדגים תהליך DevOps מלא על אפליקציה אמיתית:

| שלב | מה מומש |
|-----|---------|
| פיתוח תוכנה | Flask + MySQL + REST API + UI בעברית |
| בינה מלאכותית | Google Gemini — הזמנות אישיות בעברית |
| WhatsApp | Green API — שליחה אמיתית לנייד |
| אריזה | Docker + Docker Compose — 5 שירותים |
| Nginx | Reverse proxy + static files + DNS fix |
| Secrets | GitHub Secrets + .env + Terraform sensitive |
| בדיקות | pytest 8 tests — CRUD מלא דרך שרת + DB |
| CI | GitHub Actions — test → build → push → deploy |
| CD | SSH לHetzner (root) → rolling update ללא downtime |
| ניטור | Prometheus + Grafana — metrics בזמן אמת |
| IaC | Terraform — hcloud_server + Firewall ב-Hetzner Cloud |

### ערך נוסף

- **Mock Mode** — האפליקציה עובדת גם ללא API keys (שימושי לפיתוח ולבדיקות)
- **שפה עברית** — כל ה-UI, ההזמנות, ותזכורת הכישלון — בעברית
- **Email on failure** — CI שולח email עם פרטי הכישלון אוטומטית
- **Health endpoint** — `/api/health` לניטור ו-uptime checks

---

*ספר פרויקט — Wedding Invite Manager*
*פרויקט גמר — הנדסאי תוכנה ו-DevOps*
